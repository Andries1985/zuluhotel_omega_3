/*
        Maintened by *Edwards

        2010-11-19
*/

/*
        CUT 2026-08-26: removed from the rituals package during the revival pass,
        per the finalized cut list (Rituals Package Revival plan). Left in place,
        commented out, rather than deleted.

use uo;
use os;

include "include/string";
// include ":loot:identify";
// include ":rituals:rituals";
include "include/namingbyenchant";
include "../pkg/opt/rituals/include/rituals";

program ritual_restoration( parms )

        var mobile := parms[1],
            spell_id := parms[2];

        var item := PerformRitual( mobile, spell_id );
	if( !item )
                ChangeLightLevel( mobile, 0 );
		return 0;
	endif

        ApplyRitualEffect( mobile, item );

        return 1;
endprogram

function ApplyRitualEffect( mobile, item )

        var item_desc := TruncateArticle( item.desc );

	item.hp := item.maxhp;

	SendSysMessage( mobile, "This "+item_desc+" is now in perfect state." );

        return 1;
endfunction
*/
