ICP Register
{
	Name		QuestPkg
	Version		1.0

	Description	Generic quest package: player quest state, the paperdoll quest journal, and per-NPC quest-giver gumps. Content packages (e.g. rituals) register their own quests in quests.cfg and tag NPCs to hook into it -- no bespoke per-quest death scripts needed.

	Creator		Alryc

	// Script	cmdlevel	path
}
