// Server-boot entry point for the townstone package. Rebuilds the town list
// then launches the election watcher so any pending elections / polls that
// expired while the shard was offline finish on their next tick.

use os;

Start_Script(":townstone:townlistbootstrap");
Start_Script(":townstone:electionwatch");
