# Relocated from 0xA320 to 0x35006 2026-09-13 (crafting-station rollout
# fallout): 0xA320 is a real client graphic that the Plow station (see
# pkg/std/carpentry/itemdesc.cfg) legitimately needs as its own objtype for
# its correct in-game appearance. This item never displayed graphic 0xA320
# anyway (see the Graphic 0x0ed4 field below) -- it was just squatting on a
# spare real-graphic objtype number, exactly the scenario
# project_graphic_id_ceiling's rule covers: the OLD item using a real graphic
# ID as an arbitrary internal identifier moves, not the NEW item that needs
# it for its actual look. Zero live-placed townstones existed in
# data/items.txt at the time of this move (verified before moving). Every
# hardcoded TOWNSTONE_OBJTYPE/0xa320 reference repo-wide was updated to match
# (electionwatch.src, createtownstone.src, removetownmember.src,
# townbankstatus.src, townlistbootstrap.src, globeofsosaria.src).
CONTAINER 0x35006
{
    Name			townstone
    Desc			Town Stone
    Color			2669
    Graphic			0x0ed4
    Movable			0
    RequiresAttention	1
    Script			tstone
    Weight			5000	
    SaveOnExit		1
    Tooltip			TownStone, use to vote on  your mayor
    GUMP			0x0042
    MinX			50
    MaxX			250
    MinY			50
    MaxY			250
}


Item 0xa321
{
    Graphic		0x13b9
    Name            voterune
    Desc		voterune
    Color		120
    weight		5000
    saveonexit	1
    tooltip		This is a bug, call a GM now
}
