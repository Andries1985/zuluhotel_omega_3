################################################################
# Potion Vat -- 4 permanent choices (facing x open/closed), picked once at
# placement, never toggled afterward. Keys aren't real facing clilocs (there
# isn't one for "open" vs "closed"), so the placement gump falls back to
# plain "Facing 1".."Facing 4" labels -- see pkg/items/deed/deed/use.src's
# BuildandSendOrientGump, which already handles non-cliloc keys this way.
################################################################
Build 1
{
	Component	0xA84B 0 0 0
}
Build 2
{
	Component	0xA84C 0 0 0
}
Build 3
{
	Component	0xA84D 0 0 0
}
Build 4
{
	Component	0xA84E 0 0 0
}
