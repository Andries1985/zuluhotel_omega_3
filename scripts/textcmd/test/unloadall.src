// Synopsis: Force-unload all currently running scripts immediately
use uo;
use os;

program unloadall( who )

var res := unload_scripts("");
SendSysMessage(who, res+" scripts unloaded.");

endprogram
