// Synopsis: Remove all temporary stat modifiers from a targeted player
use os;
use uo;

include "include/attributes";

program wipemods( who )
	
	SetStrengthMod(who, 0 );
	SetDexterityMod(who, 0);
	SetIntelligenceMod(who, 0);
		
endprogram
