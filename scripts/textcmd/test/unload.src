// Synopsis: Unload all cached scripts or scripts matching a filter string
use uo;
use os;

program unload( who , text )

	if( !text )
		unload_scripts();
	else
		unload_scripts( text );
	endif

endprogram
