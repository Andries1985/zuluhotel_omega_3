// Synopsis: Display the current server system load and severity level
use uo;
use os;

program sysload(who)

	SendSysMessage (who, "Sysload is " + polcore().sysload + " (" + polcore().sysload_severity + ")" );
	
endprogram
